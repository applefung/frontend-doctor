import React, {useState, useEffect, useCallback} from 'react';
import { View, Text, Image, AsyncStorage, StyleSheet, TouchableOpacity} from 'react-native';

const Home = ()=> {
    return (
        <View>
            <Text>Home</Text>
        </View>
    );
}

export default Home;